rootProject.name = 'appsonair_flutter_appremark'
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
       // mavenLocal()    // Add this line to include your local Maven repo
    }
}
